/**
 * File:
 *   yep_conf.scr
 * Summary:
 *   SCR Agent for reading/writing /etc/yep.conf
 *   using the ini-agent
 * Access:
 *   read/write
 * Authors:
 *   Lukas Ocilka <locilka@suse.cz>
 *
 * Example:
 *   Read (.yep_conf.value.NU.NUUser)
 *   ("john_the_fish")
 *   Write (.yep_conf.value.NU.NUUser, "Howard")
 *   (true)
 *
 * $Id: $
 *
 * Read/Sets the values defined in /etc/yep.conf.
 */
.yep_conf

`ag_ini(
  `IniAgent(
    "/etc/yep.conf",
    $[
      "comments": [ "^[ \t]*#.*", "#.*", "^[ \t]*$" ],
      "sections" : [
        $[
        "begin" : [ "^[ \t]*\\[[ \t]*(.*[^ \t])[ \t]*\\][ \t]*", "[%s]" ],
        ],
      ],
      "params" : [
        $[
        "match" : [ "^[ \t]*([^=]*[^ \t=])[ \t]*=[ \t]*(.*[^ \t]|)[ \t]*$",
                    "%s = %s"],
        ],
      ],
    ]
  )
)
